"WITH SET cols AS '{PERIODS.DefaultMember.Children}'
          SET rows AS '{FCT_CC.Members}'
SELECT cols ON COLUMNS,
             NON EMPTY rows ON ROWS
FROM Functions
WHERE (Measures.HoursQty, CAT_FCT.DefaultMember, SCALING.DefaultMember)"
